Компас
*******

.. py:module:: microbit.compass

Этот модуль позволяет получить доступ к встроенному электронному компасу. Перед использованием,
компас должен быть откалиброван, иначе показания могут быть неправильными

.. warning::

    Калибровка состоит из небольшой игры, в которой нужно нарисовать круг на
    Светодиодный дисплей при вращении устройства


Функции
=========

.. py:function:: calibrate()

    апускает процесс калибровки. Инструктивное сообщение будет прокручиваться
    пользователю, после чего ему нужно будет повернуть устройство, чтобы
    нарисовать круг на светодиодном дисплее.

.. py:function:: is_calibrated()

    Возвращает ``True`` если компас был успешно откалиброван, или
    возвращается ``False``.


.. py:function:: clear_calibration()

    Отменяет калибровку.


.. py:function:: get_x()

    Дает показания магнитного поля по оси «х», как положительное или отрицательное целое число, 
    в зависимости от направления поля.


.. py:function:: get_y()

    Дает показания магнитного поля по оси «y», как положительное или отрицательное целое число, 
    в зависимости от направления поля.


.. py:function:: get_z()

    Дает показания магнитного поля по оси «z», как положительное или отрицательное целое число, 
    в зависимости от направления поля..


.. py:function:: heading()

    Дает направление по компасу, рассчитанное на основе приведенных выше показаний, в качестве
    целого числа в диапазоне от 0 до 360, представляющее угол в градусах, по часовой стрелке, 
    с севером как 0.


.. py:function:: get_field_strength()

    Возвращает целочисленное значение величины магнитного поля вокруг устройства.


Примеры
=======

.. include:: ../examples/compass.py
    :code: python
